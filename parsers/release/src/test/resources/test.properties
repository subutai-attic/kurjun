project.build.directory = ${project.build.directory}

test.release.codename = ${test.release.codename}
test.release.url      = ${test.release.url}
test.release.sign.url = ${test.release.sign.url}

test.keyring.url      = ${test.keyring.url}