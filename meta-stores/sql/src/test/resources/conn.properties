dataSourceClassName     = ${test.db.className}
dataSource.serverName   = ${test.db.server}
dataSource.databaseName = ${test.db.dbName}
dataSource.user         = ${test.db.user}
dataSource.password     = ${test.db.pass}
