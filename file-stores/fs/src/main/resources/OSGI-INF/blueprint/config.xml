<?xml version="1.0" encoding="UTF-8" ?>
<blueprint xmlns="http://www.osgi.org/xmlns/blueprint/v1.0.0">

  <reference id="kurjunProperties" interface="${project.groupId}.common.service.KurjunProperties" />

  <bean id="fsFileStoreFactory" class="${project.groupId}.storage.fs.FileSystemFileStoreFactoryImpl">
    <argument ref="kurjunProperties"/>
  </bean>

  <service id="fsFileStoreService" ref="fsFileStoreFactory"
           interface="${project.groupId}.storage.fs.FileSystemFileStoreFactory"/>

</blueprint>

